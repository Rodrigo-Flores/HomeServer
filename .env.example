# ============================================
# CONFIGURACIÓN DEL SERVIDOR MULTIMEDIA
# ============================================

# --- Identificación de Usuario ---
# Obtén estos valores ejecutando: id -u && id -g
# En Linux/Mac: id -u (PUID) y id -g (PGID)
# En Windows con WSL2: usa 1000 y 1000
PUID=1000
PGID=1000

# --- Zona Horaria ---
# Lista completa: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# --- Puertos de Servicios ---
JELLYFIN_PORT=8096
RADARR_PORT=7878
SONARR_PORT=8989
PROWLARR_PORT=9696
QBITTORRENT_PORT=8080
QBITTORRENT_TORRENT_PORT=6881
BAZARR_PORT=6767

# --- Límites de Memoria (Docker Deploy) ---
# Ajusta estos valores según tu disponibilidad de RAM
# Formato: 512M, 1G, 2G, etc.

# Jellyfin (Servidor de Streaming)
JELLYFIN_MEMORY_LIMIT=4G
JELLYFIN_MEMORY_RESERVATION=2G

# Radarr (Gestor de Películas)
RADARR_MEMORY_LIMIT=1G
RADARR_MEMORY_RESERVATION=512M

# Sonarr (Gestor de Series)
SONARR_MEMORY_LIMIT=1G
SONARR_MEMORY_RESERVATION=512M

# Prowlarr (Indexador)
PROWLARR_MEMORY_LIMIT=512M
PROWLARR_MEMORY_RESERVATION=256M

# qBittorrent (Cliente Torrent)
QBITTORRENT_MEMORY_LIMIT=2G
QBITTORRENT_MEMORY_RESERVATION=1G

# Bazarr (Gestor de Subtítulos)
BAZARR_MEMORY_LIMIT=512M
BAZARR_MEMORY_RESERVATION=256M

# --- Rutas de Datos ---
# Cambia estas rutas cuando migres a un disco externo
# Ejemplos:
#   - macOS: /Volumes/MiDiscoExterno/HomeServer
#   - Linux: /mnt/disco/HomeServer
#   - Windows: D:/HomeServer
DATA_PATH=./data
CONFIG_PATH=./config

# --- URL Publicada de Jellyfin ---
# Usa localhost para acceso local únicamente
# Para acceso desde otros dispositivos en la red, usa tu IP local
# Si usas Caddy con SSL, usa tu dominio con https://
# Obtén tu IP: 
#   - macOS/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
#   - Windows: ipconfig
JELLYFIN_PUBLISHED_URL=http://localhost:8096
# JELLYFIN_PUBLISHED_URL=https://tu-dominio.ddns.net  # Descomentar si usas Caddy

# --- Caddy (Reverse Proxy con SSL) ---
# Solo necesario si habilitas el servicio Caddy en docker-compose.yml
CADDY_MEMORY_LIMIT=256M
CADDY_MEMORY_RESERVATION=128M
